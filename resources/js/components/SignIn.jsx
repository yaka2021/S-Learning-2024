import { useNavigate } from "react-router-dom"; //ページ遷移を実装するために必要


export const SignIn = () => {
    const navigate = useNavigate();

    return (
        <>
        <main>
        </main>
        </>
    );
}
